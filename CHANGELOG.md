2026-04-19  Ryan Musante

- v3.1: 5 `.opt` files modified (net +0 keys: -1 Genesis lowpass_range + 1 PCSX frameskip_threshold); 5 README table rows updated to reflect max-performance baseline. **Genesis Plus GX.opt (-1 key, 1 value change):** `genesis_plus_gx_audio_filter = "low-pass"` → `"off"` (removes per-sample IIR low-pass filter on MD/Genesis audio for CPU headroom); `genesis_plus_gx_lowpass_range = "55"` removed (inert when filter is off; 55 Hz authentic MD roll-off no longer applied). Header comment updated "MAME YM2612, low-pass 55Hz, no sprite limit, per-game BRAM, single-field render" → "MAME YM2612, audio filter off (CPU headroom), no sprite limit, per-game BRAM, single-field render". `Genesis Plus GX.opt` key count 7 → 6. **mGBA.opt (2 value changes):** `mgba_interframe_blending = "mix_smart"` → `"disabled"` (removes per-frame GPU blend on GBA; flicker returns on alternate-frame titles — Zelda Minish Cap, F-Zero GP Legend, MK Super Circuit); `mgba_audio_low_pass_filter = "enabled"` → `"disabled"` (removes IIR filter on GBA stereo; brighter/harsher audio). Header comment updated. `mGBA.opt` key count unchanged at 3. **PCSX-ReARMed.opt (+1 key, 1 value change):** `pcsx_rearmed_frameskip_type = "disabled"` → `"auto_threshold"`; new key `pcsx_rearmed_frameskip_threshold = "33"` added (frameskip triggers at 33% audio-buffer threshold; guarantees real-time playback on 3D-heavy titles — Spyro 2/3, Tony Hawk, Tekken 3 — at the cost of visible frame drops under load instead of audio slowdown; compensates for no-JIT interpreter stack on A15). Header comment updated "DRC off, no frameskip, async GPU, psxclock 100, cd_readahead 333000 (full-disk precache, ~780 MB RAM)" → "DRC off, auto_threshold frameskip @ 33% buffer (interpreter safety on no-JIT A15), async GPU, psxclock 100, cd_readahead 333000 (full-disk precache, ~780 MB RAM)". `PCSX-ReARMed.opt` key count 5 → 6. **Beetle PCE Fast.opt (1 value change):** `pce_fast_cdspeed = "2"` → `"4"` (raises CD streaming speed from 2× to 4×; timing-sensitive CD audio titles — Ys IV, Dracula X — may desync; revert to `"2"` per-game if observed). Header comment updated. `Beetle PCE Fast.opt` key count unchanged at 3. **Mesen.opt (1 value change):** `mesen_reduce_dmc_popping = "enabled"` → `"disabled"` (removes per-sample APU DMC correction for sub-1% CPU saving; audible pops possible on DPCM-heavy titles — Skate or Die 2, Ninja Gaiden III). Header comment updated. `Mesen.opt` key count unchanged at 2. Total opt key count across all 8 cores unchanged at 27 (Genesis -1, PCSX +1, net 0); total cfg key count unchanged at 49. **README updates:** §1 Supported Cores table Beetle PCE Fast row Notes — appended `pce_fast_cdspeed = "4"` clause with v3.1 marker, Ys IV / Dracula X desync caveat, per-game revert guidance. §1 Supported Cores table Genesis Plus GX row — `.opt Keys` column `7` → `6`; Notes rewritten to flag `audio_filter` low-pass → off + inert lowpass_range key removal with v3.1 marker; removed stale "thermal-safe; Nuked per-game" parenthetical. §1 Supported Cores table Mesen row Notes — appended `mesen_reduce_dmc_popping` enabled → disabled clause with v3.1 marker, DPCM-heavy pop caveat, per-game restore guidance. §1 Supported Cores table mGBA row Notes — replaced `interframe_blending = "mix_smart"` preservation clause with `mgba_interframe_blending` disabled clause + `mgba_audio_low_pass_filter` disabled clause, both with v3.1 markers; flicker caveat moved to v3.1 clause. §1 Supported Cores table PCSX-ReARMed row — `.opt Keys` column `5` → `6`; Notes appended `pcsx_rearmed_frameskip_type` disabled → auto_threshold + new `pcsx_rearmed_frameskip_threshold = "33"` key with v3.1 marker, Spyro/THPS/Tekken benefit + frame-drop tradeoff explanation. §5 Shaders section — first sentence rewritten to reflect companion v3.1 `video_shader_enable = "false"` default ("shaders disabled by default for max GPU headroom at 4K60"); preset path described as inert fallback; crt-easymode default selection history retained for users who flip the gate back on. All other 11 `.cfg`/`.opt` file contents unchanged from v3.0. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.54 block removed). Version badge 3.0 → 3.1. Companion `retroarch-appletv4k` bumped to v3.1 (5 `retroarch.cfg` value changes: `video_shader_enable` true → false, `fastforward_ratio` 4.0 → 0.0, `autosave_interval` 60 → 300, `audio_resampler_quality` 2 → 1, `savestate_thumbnail_enable` true → false; global key count unchanged at 75; landing paragraph + §7 video/latency/additional tables + §7 save-state callout + §8 Shaders intro + §11 checklist updated to match).

2026-04-19  Ryan Musante

- v3.0: MAJOR version-sync release. Establishes lockstep versioning across companion repositories: both `retroarch-configs` and `retroarch-appletv4k` now share a single MAJOR.MINOR tag and will bump together on every future release regardless of which side contains the real file changes. Pre-sync state: `retroarch-configs` at v1.57, `retroarch-appletv4k` at v2.95 (the two repos had evolved independently since inception with asymmetric version numbering). No `.cfg`/`.opt` changes, no per-core key drift — all 16 file contents byte-identical to the v1.57 shipping state. **README updates:** §11 Versioning — policy text revised to document the lockstep convention explicitly, matching companion `retroarch-appletv4k` §13 byte-for-byte. Prior text: "This repository uses `vMAJOR.MINOR` (no patch component). `MAJOR` increments on incompatible structural changes (filesystem layout, breaking config schema, removed features)." New text prepends a lockstep-scope sentence and expands the MAJOR clause to include cross-repo version-sync events: "This repository uses `vMAJOR.MINOR` (no patch component) in lockstep with its companion repository — both `retroarch-appletv4k` and `retroarch-configs` share a single MAJOR.MINOR tag that increments together on every release, regardless of which side contains the real file changes. `MAJOR` increments on incompatible structural changes (filesystem layout, breaking config schema, removed features) or cross-repo version-sync events (e.g. v3.0 re-aligned the two repos after they had evolved independently at v2.95 / v1.57)." MINOR clause, CHANGELOG GNU-style-date convention, and 5-entry trim policy unchanged. Total cfg key count across all 8 cores unchanged at 49; total opt key count unchanged at 27. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.53 block removed). Version badge 1.57 → 3.0. Companion `retroarch-appletv4k` bumped to v3.0 (matching sync; §13 Versioning text revised identically; `retroarch.cfg` byte-identical to v2.95 shipping state).

2026-04-19  Ryan Musante

- v1.57: 2 `.cfg` files modified, 1 `.opt` file modified. Removes Mupen64Plus-Next per-core stutter-mitigation tax introduced in v1.56; applies two max-performance tunes to Beetle PCE Fast. **Removes (Mupen64Plus-Next.cfg, -2 keys):** `audio_latency = "64"` (Mupen now inherits global `48` from the v2.94-reverted baseline; no per-core audio buffer padding). `video_max_swapchain_images = "3"` (Mupen now inherits global double-buffer `2`; no per-core output-latency penalty). Mupen64Plus-Next.cfg key count 7 → 5. Header comment reverted to "Tier 2 N64 frontend pins: video_threaded off, video_frame_delay_auto off, rewind off, run_ahead off" (drops the audio_latency + swapchain mention). Rationale: the v1.56 stutter mitigation penalized Mupen with ~48 ms additional audio latency and ~16 ms additional output latency — costs that only pay off if CPU underruns are actually occurring. The v1.56 `"all threads"` angrylion-multithread pin (retained) already extracts maximum parallelism from the A15; combined with the v2.94-restored minimum-latency global baseline, Mupen should be left at the same low-latency posture as every other core. If underruns prove unavoidable in specific heavy ROMs, mitigation belongs in per-game Save Game Options, not in a per-core blanket pin. **Modifies (Beetle PCE Fast.opt, 1 value):** `pce_fast_cdimagecache = "disabled"` → `"enabled"` (loads the full CD image into RAM at content launch; eliminates disc seek latency during gameplay; startup load is slightly longer but gameplay is fully precached; aligns with PCSX-ReARMed `pcsx_rearmed_cd_readahead = "333000"` full-precache architecture; 64 GB ATV4K has ample cache for typical PCE CD image sizes 50–700 MB). Header comment updated. Opt key count `Beetle PCE Fast.opt` unchanged at 3. **Modifies (Beetle PCE Fast.cfg, 1 value):** `run_ahead_frames = "1"` → `"2"` (brings PCE Fast to parity with the 6 other Tier 1 cores which all run 2 frames). The prior 1-frame cap was justified in-repo as "CDROM seek determinism," but this was over-conservative — libretro/beetle-pce-fast-libretro issue #127 confirms 2-frame run-ahead works correctly on PCE CD games *as long as Run Ahead Secondary Instance is off*, and this repo has pinned `run_ahead_secondary_instance = "false"` on Beetle PCE Fast since v1.00. The v1.57 cdimagecache enable further eliminates disc-seek nondeterminism as a theoretical concern. Header comment updated. Cfg key count `Beetle PCE Fast.cfg` unchanged at 6. Total cfg key count across all 8 cores 51 → 49 (Mupen -2); total opt key count unchanged at 27. **README updates:** §1 Supported Cores Beetle PCE Fast row Notes — "Integer overscale 256×240 (+ 512-wide hi-res) @ 4K; Run Ahead 1 frame (CDROM seek determinism)" → "Integer overscale 256×240 (+ 512-wide hi-res) @ 4K; Run Ahead 2 frames (single instance; CD games verified safe per libretro/beetle-pce-fast-libretro#127); `pce_fast_cdimagecache = \"enabled\"` (full CD image precache to RAM; eliminates disc seek latency during gameplay)". §1 Supported Cores Mupen64Plus-Next row — `.cfg Keys` column `7` → `5`; Notes: dropped the "per-core `.cfg` pins localize stutter mitigation off the global baseline: `audio_latency = \"64\"`..., `video_max_swapchain_images = \"3\"`..." clause; added trailing "Mupen inherits global low-latency posture (`audio_latency = 48`, `video_max_swapchain_images = 2`) — no per-core stutter-mitigation tax". §4 Frontend Override Keys table `audio_latency` row — Values Used `48, 64` → `48`; Purpose reverted from "PCSX-ReARMed pin `48` (drift-guard on global); Mupen64Plus-Next pin `64` (+16 ms buffer...)" to "PCSX-ReARMed pin (drift-guard on global); all other cores inherit global `48` (minimum viable buffer on tvOS coreaudio)". §4 Frontend Override Keys — `video_max_swapchain_images` row removed entirely (no per-core pins remain; all cores inherit global double-buffer `2`). All other 13 file contents unchanged from v1.56. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.52 block removed). Version badge 1.56 → 1.57. Companion `retroarch-appletv4k` bumped to v2.95 (SYNC — no `retroarch.cfg` changes, README badge only; CHANGELOG entry describes the companion v1.57 changes for cross-reference; global key count unchanged at 75).

2026-04-19  Ryan Musante

- v1.56: 1 `.cfg` file modified (+2 keys), 1 `.opt` file modified (value change). Localizes N64 stutter mitigation to Mupen64Plus-Next per-core overrides so the companion global `retroarch.cfg` can revert to v2.92 low-latency baseline (see `retroarch-appletv4k` v2.94). **Adds (Mupen64Plus-Next.cfg, +2 keys):** `audio_latency = "64"` (+16 ms buffer over the v2.94-reverted global 48 ms; tolerates CPU underruns on the interpreter-only N64 core under heavy scenes; all other cores retain global 48 for minimum latency). `video_max_swapchain_images = "3"` (triple-buffer swap chain; one frame of pacing slack so the CPU-bound core drops a frame instead of glitching mid-scanout when it misses a vsync deadline; ~16 ms output-latency cost localized to Mupen only — Tier 1 + PCSX retain double-buffer). Mupen64Plus-Next.cfg key count 5 → 7. Header comment updated to list the new pins. **Modifies (Mupen64Plus-Next.opt, value):** `mupen64plus-angrylion-multithread = "3"` → `"all threads"` (upstream default per M4xw at Libretro; angrylion frame completion is gated by the slowest RDP worker, so idle cores are wasted parallelism; scheduler-contention concerns on the heterogeneous A15 cluster are theoretical on a dedicated-function console with no foreground competition — confirmed value from libretro/mupen64plus-libretro-nx issue #158). Header comment updated. Opt key count `Mupen64Plus-Next.opt` unchanged at 6; total opt key count across all 8 cores unchanged at 27; total cfg key count across all 8 cores 49 → 51. **README updates:** §1 Supported Cores table Mupen64Plus-Next row — `.cfg Keys` column `5` → `7`; Notes: multithread value `"3"` → `"all threads"`; rationale rewritten from "ATV4K binned A15 = 2P+3E; 3 workers = 2P + 1E..." to "upstream default; maximizes software-rasterizer parallelism across all available cores — frame completion is gated by slowest RDP worker, so any idle core is wasted parallelism; dedicated-function console has no foreground competition for scheduler contention"; added the two new per-core pins (`audio_latency = "64"`, `video_max_swapchain_images = "3"`) with one-line rationale each; existing pins (`video_threaded`, `video_frame_delay_auto`, `rewind_enable`, `run_ahead_enabled`) retained. §4 Frontend Override Keys table `audio_latency` row — Values Used column `48` → `48, 64`; Purpose column expanded from "PCSX-ReARMed pin; matches global v2.66 (down from 64 ms); Mupen inherits global" to "PCSX-ReARMed pin `48` (drift-guard on global); Mupen64Plus-Next pin `64` (+16 ms buffer for CPU-underrun tolerance on interpreter-only N64 stack)". §4 new row: `video_max_swapchain_images` — Values Used `3`; Purpose "Mupen64Plus-Next pin only (triple-buffer for pacing slack on CPU-bound core; all other cores inherit global double-buffer `2` for minimum output latency)". All other 14 file contents unchanged from v1.55. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.51 block removed). Version badge 1.55 → 1.56. Companion `retroarch-appletv4k` bumped to v2.94 (3 `retroarch.cfg` value reverts to v2.92 baseline: `audio_latency` 96 → 48, `audio_rate_control_delta` 0.012 → 0.008, `video_max_swapchain_images` 3 → 2; thermal-language cleanup across §7 Latency reduction and Additional settings tables; global key count unchanged at 75).

2026-04-19  Ryan Musante

- v1.55: 1 `.opt` value change targeting N64 Mupen64Plus-Next audio stutter on the Angrylion software RDP stack. **Modifies:** `Mupen64Plus-Next.opt` `mupen64plus-angrylion-multithread = "2"` → `"3"` (adds one additional RDP worker thread; on the ATV4K binned A15 (2P+3E), 3 workers pulls in one E-core on top of the two P-cores — trades a modest amount of P-core pacing smoothness for extra software-rasterizer parallelism to reduce stutter under heavy 3D scenes; still well below the default "all threads" (5) that oversubscribes the E-cluster). Header comment updated "multithread=2 (A15 P-cores)" → "multithread=3 (2 P-cores + 1 E-core)". Opt key count `Mupen64Plus-Next.opt` unchanged at 6; total opt key count across all 8 cores unchanged at 27; cfg key count unchanged at 49. **README updates:** §1 Supported Cores table Mupen64Plus-Next row Notes — `mupen64plus-angrylion-multithread = "2"` → `"3"`; parenthetical rationale reworded from "ATV4K binned A15 = 2P+3E; default \"all threads\" oversubscribes E-cores, harming frame pacing vs 2 P-core workers" to "ATV4K binned A15 = 2P+3E; 3 workers = 2P + 1E, gaining one E-core of RDP parallelism for stutter mitigation without reaching the default \"all threads\" (5) that oversubscribes the E-cluster and harms pacing". All other 15 file contents unchanged from v1.54. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.50 block removed). Version badge 1.54 → 1.55. Companion `retroarch-appletv4k` bumped to v2.93 (3 `retroarch.cfg` value changes: `audio_latency` 48 → 96, `audio_rate_control_delta` 0.008 → 0.012, `video_max_swapchain_images` 2 → 3; global key count unchanged at 75).

2026-04-18  Ryan Musante

