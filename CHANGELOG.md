2026-04-19  Ryan Musante

- v1.54: SYNC — no `.cfg` / `.opt` changes, no README changes. Version tag aligns the companion release channel with `retroarch-appletv4k` v2.92, which modifies `fastforward_ratio` from `"3.0"` to `"4.0"` in the global `retroarch.cfg` (raises the fast-forward speed cap from 3× to 4×; all 8 per-core cfgs inherit the global). Per-core frontend overrides do not set `fastforward_ratio` — the new cap is inherited from global across all 8 cores. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.49 block removed). Version badge 1.53 → 1.54. Companion `retroarch-appletv4k` bumped to v2.92 (`fastforward_ratio` 3.0 → 4.0; global key count unchanged at 75).

2026-04-19  Ryan Musante

- v1.53: 1 `.opt` file modified, -1 key. **Removes:** `PCSX-ReARMed.opt` L6 `pcsx_rearmed_neon_enhancement_enable = "disabled"` — pin is permanently inert on aarch64 tvOS 26 (ARM NEON enhancement code path is 32-bit-only; upstream default on aarch64 is also effectively disabled); removal drops dead weight without behavior change. Opt key count `PCSX-ReARMed.opt` 6 → 5; total opt key count across all 8 cores 28 → 27; cfg key count unchanged at 49. **Modifies (comment-only, parser-inert):** `PCSX-ReARMed.opt` header comment expanded from one line to two — L1 updated "~750 MB RAM" → "~780 MB RAM" (333000 sectors × 2352 B ≈ 780 MB, corrected arithmetic); L2 added as a blocking WARNING that `cd_readahead = "333000"` is outside the Core Options menu enumerated range and menu-save (Quick Menu → Options → Manage Core Options → Save) will silently replace it with the nearest dropdown value (typically 768, reducing precache to ~1.8 MB) and break the full-disk precache strategy. **README updates:** §1 Supported Cores table PCSX-ReARMed row — `.opt Keys` column 6 → 5; Notes column `cd_readahead` trailing parenthetical extended to "(full-disk precache; ~780 MB RAM; eliminates CHD seek latency; **outside Core Options menu enum — never open Quick Menu → Options → Manage Core Options → Save for this core, or the pin is silently replaced with the nearest dropdown value**)". `.cfg` key counts and all other 15 file contents unchanged from v1.52. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.48 block removed). Version badge 1.52 → 1.53. Companion `retroarch-appletv4k` bumped to v2.91 (retroarch.cfg L1 single-line header comment added (parser-inert) matching the per-core header convention; README §7 Hotkeys save-state behavior note reworded to separate flush semantics (`autosave_interval`) from no-overwrite guard semantics (`block_sram_overwrite`); global key count unchanged at 75).

2026-04-19  Ryan Musante

- v1.52: SYNC — no `.cfg` / `.opt` changes, no README changes. Version tag aligns the companion release channel with `retroarch-appletv4k` v2.90, which modifies `savestate_max_keep` from `"5"` to `"10"` in the global `retroarch.cfg` (doubles the auto-indexed state slot ring). Per-core frontend overrides do not touch `savestate_max_keep` — the new cap is inherited from global across all 8 cores. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.47 block removed). Version badge 1.51 → 1.52. Companion `retroarch-appletv4k` bumped to v2.90 (`savestate_max_keep` 5 → 10; global key count unchanged at 75).

2026-04-19  Ryan Musante

- v1.51: SYNC — no `.cfg` / `.opt` changes, no README changes. Version tag aligns the companion release channel with `retroarch-appletv4k` v2.89, which adds two `savestate_auto_*` pins (`savestate_auto_save = "true"`, `savestate_auto_load = "false"`) to the global `retroarch.cfg`. Per-core frontend overrides do not touch `savestate_auto_*` keys — the new behavior is inherited from global across all 8 cores. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.46 block removed). Version badge 1.50 → 1.51. Companion `retroarch-appletv4k` bumped to v2.89 (`savestate_auto_save` on, `savestate_auto_load` off; global key count 73 → 75).

2026-04-19  Ryan Musante

- v1.50: Documentation sync — no `.cfg` / `.opt` changes. §5 Shaders paragraph updated to reflect companion `retroarch-appletv4k` v2.88 shader swap: global `video_shader` path `../shaders/shaders_slang/crt/crt-aperture.slangp` → `../shaders/shaders_slang/crt/crt-easymode.slangp`; "as of companion v2.84" → "as of companion v2.88"; rationale added (integer-scale safe, no shader geometry, cleaner 4K phosphor mask); two references to "the global `crt-aperture` preset" in the same paragraph updated to `crt-easymode`. `handheld/lcd-grid-v2.slangp` guidance for mGBA retained unchanged. All 16 `.cfg` / `.opt` file contents, key counts (49 cfg + 28 opt), and key values unchanged from v1.49. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.45 block removed). Version badge 1.49 → 1.50. Companion `retroarch-appletv4k` bumped to v2.88 (global shader swap crt-aperture → crt-easymode; 3 drift-guard pins added: `video_vsync`, `sort_savefiles_enable`, `sort_savestates_enable`; README intro/§3/§4 directory phrasing trim; §7 VSync + Sort Save Files/States rows added; §8 shader documentation rewritten; §11 checklist updated; global key count 70 → 73).

2026-04-18  Ryan Musante

