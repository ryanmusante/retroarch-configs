2026-04-19  Ryan Musante

- v3.3: 2 `.cfg` files modified (+6 keys total: Mupen +4, PCSX +2), 1 `.opt` file modified (1 value change). Restores Mupen64Plus-Next stutter mitigations dropped in v1.57 without measured evidence; adds two new Mupen pins for audio-pacing and purgeable-cache stall avoidance; establishes Tier 2 parity on PCSX-ReARMed for the same frame-time variance absorption; corrects Angrylion multithread value for A15's heterogeneous 2P+4E cluster. **Mupen64Plus-Next.cfg (+4 keys):** `audio_latency = "96"` (HIGH; +32 ms absorption over global 64 ms for E-core stragglers + GC + OS scheduler variance on heterogeneous 2P+4E A15; 64 ms ~= 3.8 frames @ 60 Hz, one E-core RDP straggler at ~20 ms + scheduler 5–10 ms + GC 5 ms consumes the whole buffer — 96 ms ~= 5.8 frames adds ~32 ms headroom covering these sources exactly). `video_max_swapchain_images = "3"` (HIGH; absorbs frame-time variance on CPU-bound Tier 2 core; triple-buffer swap chain with Metal driver has no pacing penalty on fixed-refresh tvOS; one extra 320×240 framebuffer is trivial memory on 4 GB device; ~16 ms output-latency cost acceptable on Tier 2). `audio_sync = "false"` (HIGH; on frame-budget overrun under global `audio_sync = "true"` the frontend throttles frame delivery to audio rate, producing pitch rubber-band / wobble characteristic of observed stutter; video-driven pacing substitutes clean dropped frames for audio warping — preferable for listenability; pitch stays stable, only pace varies). `autosave_interval = "0"` (HIGH; SRAM write every 2.5 min touches purgeable cache on 64 GB model = measurable ~16–40 ms stall mid-play; save-on-close retained via companion `savestate_auto_save = "true"` global). Mupen64Plus-Next.cfg key count 5 → 9. Header comment rewritten from "Tier 2 N64 frontend pins: video_threaded off, video_frame_delay_auto off, rewind off, run_ahead off" to full one-line rationale documenting platform-forced plugin stack (Angrylion+CXD4+cached_interpreter — GLideN64 needs GL context the Metal driver does not provide, Parallel-RDP needs Vulkan, Parallel-RSP + dynamic_recompiler need JIT disallowed by App Store), retained upstream-issue pin references (#14978, #14201, #18300), v1.56 pin restoration rationale, and new audio_sync + autosave_interval pin rationale. **Mupen64Plus-Next.opt (1 value change):** `mupen64plus-angrylion-multithread = "all threads"` → `"2"` (HIGH; A15 Bionic is 2P + 4E; "all threads" dispatches 6 RDP workers, the 4 E-core workers run at ~35–40% of P-core throughput, per-tile RDP work is slowest-thread-wins so tail latency is set by E-core finish time and presents as audio stutter + frame-time jitter; "2" pins RDP workers to P-cores for per-tile pacing consistency; fallbacks if too serial: `"3"` (v2.93 pre-"all-threads" default) then `"4"` (balance); the v1.56 `"3"` → `"all threads"` change was upstream-default-alignment reasoning from M4xw at libretro/mupen64plus-libretro-nx#158 targeting desktop homogeneous cores and is inappropriate for heterogeneous ARM mobile SoC on a dedicated-function console — the apparent parallelism gain from idle-core utilization is erased by E-core tail latency). Header comment rewritten to document platform-forced plugin stack (rdp-plugin=angrylion is the only non-GL/non-Vulkan RDP option on Metal-only build; rsp-plugin=cxd4 is the only viable RSP under Angrylion — Parallel-RSP needs JIT, HLE RSP will not initialize with Angrylion per libretro upstream; cpucore=cached_interpreter is the ceiling without JIT — dynamic_recompiler is a no-op under App Store sandbox) and angrylion-multithread heterogeneous-ARM tuning rationale with fallback values. Mupen64Plus-Next.opt key count unchanged at 6. **PCSX-ReARMed.cfg (+2 keys):** Tier 2 parity with Mupen stutter mitigations. `video_max_swapchain_images = "3"` (MED; absorbs frame-time variance on CPU-bound no-JIT interpreter stack; Metal triple-buffer has no pacing penalty on fixed-refresh tvOS). `autosave_interval = "0"` (MED; prevents PS1 memcard-write-to-purgeable-cache stall every 2.5 min mid-play on 64 GB model; save-on-close retained via global `savestate_auto_save`). `audio_sync` deliberately NOT pinned per-core on PCSX — the core's own `pcsx_rearmed_frameskip_type = "auto_threshold"` already handles audio-buffer pressure by dropping frames at 33% buffer threshold, orthogonal to the frontend audio_sync toggle; retaining global `audio_sync = "true"` preserves the pairing with core-level buffer-triggered frameskip. PCSX-ReARMed.cfg key count 7 → 9. Header comment appended with v3.3 pin rationale and audio_sync-remains-global explanation. Total cfg key count across all 8 cores 49 → 55 (Mupen +4, PCSX +2); total opt key count unchanged at 27. **README updates:** §1 Supported Cores Mupen64Plus-Next row — `.cfg Keys` column `5` → `9`; Notes rewritten to document platform-forced Angrylion+CXD4+cached_interpreter stack on Metal-only (GLideN64 needs GL context RetroArch tvOS Metal driver does not provide — no gl/glcore/gl2 driver or ANGLE bridge; Parallel-RDP needs Vulkan; Parallel-RSP + dynamic_recompiler need JIT disallowed by App Store; platform-forced, not a choice), v3.3 angrylion-multithread value change `"all threads"` → `"2"` with P-core-pin rationale and fallback values, v1.56 pin restoration (audio_latency=96 + video_max_swapchain_images=3) with absorption-headroom rationale, new pins (audio_sync=false + autosave_interval=0) with rationale. §1 Supported Cores PCSX-ReARMed row — `.cfg Keys` column `7` → `9`; Notes appended with v3.3 video_max_swapchain_images=3 Tier 2 parity + autosave_interval=0 memcard-stall-prevention pin clauses plus rationale that audio_sync stays global `true` due to core-level frameskip interaction. §4 Frontend Override Keys table `audio_latency` row — Values Used `48` → `48, 96`; Purpose expanded to distinguish PCSX-ReARMed tighter Tier 2 pin (48) from Mupen64Plus-Next absorption-headroom pin (96) for E-core stragglers + GC + scheduler variance. §4 Frontend Override Keys — new row `video_max_swapchain_images` Values `3`; Purpose documenting Tier 2 per-core pins on Mupen + PCSX (triple-buffer absorbs frame-time variance on CPU-bound no-JIT interpreter stack; Tier 1 inherits global double-buffer `2`). §4 Frontend Override Keys — new row `audio_sync` Values `false`; Purpose documenting Mupen64Plus-Next pin only (video-driven pacing substitutes clean dropped frames for pitch rubber-band under global `true`; PCSX retains global `true` because core-level `pcsx_rearmed_frameskip_type = auto_threshold` handles buffer pressure via frameskip instead). §4 Frontend Override Keys — new row `autosave_interval` Values `0`; Purpose documenting Mupen + PCSX Tier 2 pins (prevents SRAM/memcard-write-to-purgeable-cache stall every 2.5 min mid-play on 64 GB model; save-on-close retained via global `savestate_auto_save`). §4 Frontend Override Keys `run_ahead_frames` row — Values Used `1, 2` → `2`; Purpose updated to "All 7 Tier 1 cores at 2-frame parity" (fixes stale PCE Fast=1 reference; PCE raised to 2 in companion v2.95 per libretro/beetle-pce-fast-libretro#127 CD-safe-at-2-frames-single-instance confirmation — row was never updated when the value changed). All other 6 `.cfg` files (Beetle PCE Fast, FinalBurn Neo, Genesis Plus GX, Mesen, Snes9x, mGBA) and 8 `.opt` files unchanged from v3.2 byte-identically. Version badge 3.2 → 3.3. Companion `retroarch-appletv4k` bumped to v3.3 (1 `retroarch.cfg` value change: `fastforward_ratio` 0.0 → 5.0; global key count unchanged at 65; §7 Fast Forward row + §7 Audio Latency / Max Swapchain Images / Audio Sync rows + §7 Hotkeys save state behavior callout + §9 Mupen/PCSX row Notes updated to match).

2026-04-19  Ryan Musante

- v3.2: 2 `.opt` files modified — correctness-only fixes replacing display-label strings with valid internal enum values. No key count changes, no `.cfg` changes. **mGBA.opt (1 value change):** `mgba_color_correction = "Game Boy Advance"` → `"Auto"` (HIGH; prior value was the display *label*, not the internal enum. Core options in mGBA-libretro are defined as `{ value, label }` tuples — valid internal values are `OFF` / `GBA` / `GBC` / `Auto`. RetroArch's `core_option_manager.c` matches `config_set_string` output against internal values via `string_is_equal`, so the label `"Game Boy Advance"` silently failed match and fell back to `option->default_index = 0` which is `OFF` — color correction has been effectively *disabled* on mGBA since this value was introduced. `Auto` chosen over `GBA` because mGBA in this pack handles GB/GBC/GBA (per README §1 Supported Cores) — `Auto` applies the correct per-system correction rather than forcing GBA washed-out-LCD correction onto GBC content. Real behavior change: color correction now actually applies). Header comment updated "GBA core options: color correction, no interframe blending (max perf), audio low-pass filter off" → "GB/GBC/GBA core options: auto per-system color correction, no interframe blending (max perf), audio low-pass filter off". `mGBA.opt` key count unchanged at 3. **Genesis Plus GX.opt (1 value change):** `genesis_plus_gx_audio_filter = "off"` → `"disabled"` (MED; prior value `"off"` was not in the option's internal enum `disabled` / `low-pass` / `EQ` and silently fell back to default `disabled` — which happened to match intent ("audio filter off") per v3.1 changelog, so no observable behavior change. Correctness-only fix so the key actually parses as written against the core's option definitions). Header comment unchanged (already reads "audio filter off" which remains accurate). `Genesis Plus GX.opt` key count unchanged at 6. Total opt key count across all 8 cores unchanged at 27; total cfg key count unchanged at 49. **README updates:** §1 Supported Cores Genesis Plus GX row Notes — appended v3.2 marker documenting the `"off"` → `"disabled"` correctness fix with enum-fallback rationale. §1 Supported Cores mGBA row Notes — appended v3.2 marker documenting the `"Game Boy Advance"` → `"Auto"` fix with enum-mismatch explanation and per-system rationale for `Auto` over `GBA`. All other 14 `.cfg`/`.opt` file contents unchanged from v3.1. Version badge 3.1 → 3.2. Companion `retroarch-appletv4k` bumped to v3.2 (no `retroarch.cfg` value changes; header comment version stamps bumped; 2 stale README references removed — §7 Video Max Swapchain Images row Mupen-pins-3 clause (the pin was removed in companion v1.57) and §7 Latency reduction Run-Ahead Mode row "Beetle PCE Fast = 1 (CDROM)" clause (PCE Fast moved to parity at 2 frames in companion v2.95); landing paragraph version bumped; global key count unchanged at 65).

2026-04-19  Ryan Musante

- v3.1: 5 `.opt` files modified (net +0 keys: -1 Genesis lowpass_range + 1 PCSX frameskip_threshold); 5 README table rows updated. **Genesis Plus GX.opt (-1 key, 1 value change):** `genesis_plus_gx_audio_filter = "low-pass"` → `"off"` (removes per-sample IIR low-pass filter on MD/Genesis audio for CPU headroom); `genesis_plus_gx_lowpass_range = "55"` removed (inert when filter is off; 55 Hz authentic MD roll-off no longer applied). Header comment updated "MAME YM2612, low-pass 55Hz, no sprite limit, per-game BRAM, single-field render" → "MAME YM2612, audio filter off (CPU headroom), no sprite limit, per-game BRAM, single-field render". `Genesis Plus GX.opt` key count 7 → 6. **mGBA.opt (2 value changes):** `mgba_interframe_blending = "mix_smart"` → `"disabled"` (removes per-frame GPU blend on GBA; flicker returns on alternate-frame titles — Zelda Minish Cap, F-Zero GP Legend, MK Super Circuit); `mgba_audio_low_pass_filter = "enabled"` → `"disabled"` (removes IIR filter on GBA stereo; brighter/harsher audio). Header comment updated. `mGBA.opt` key count unchanged at 3. **PCSX-ReARMed.opt (+1 key, 1 value change):** `pcsx_rearmed_frameskip_type = "disabled"` → `"auto_threshold"`; new key `pcsx_rearmed_frameskip_threshold = "33"` added (frameskip triggers at 33% audio-buffer threshold; guarantees real-time playback on 3D-heavy titles — Spyro 2/3, Tony Hawk, Tekken 3 — at the cost of visible frame drops under load instead of audio slowdown; compensates for no-JIT interpreter stack on A15). Header comment updated "DRC off, no frameskip, async GPU, psxclock 100, cd_readahead 333000 (full-disk precache, ~780 MB RAM)" → "DRC off, auto_threshold frameskip @ 33% buffer (interpreter safety on no-JIT A15), async GPU, psxclock 100, cd_readahead 333000 (full-disk precache, ~780 MB RAM)". `PCSX-ReARMed.opt` key count 5 → 6. **Beetle PCE Fast.opt (1 value change):** `pce_fast_cdspeed = "2"` → `"4"` (raises CD streaming speed from 2× to 4×; timing-sensitive CD audio titles — Ys IV, Dracula X — may desync; revert to `"2"` per-game if observed). Header comment updated. `Beetle PCE Fast.opt` key count unchanged at 3. **Mesen.opt (1 value change):** `mesen_reduce_dmc_popping = "enabled"` → `"disabled"` (removes per-sample APU DMC correction for sub-1% CPU saving; audible pops possible on DPCM-heavy titles — Skate or Die 2, Ninja Gaiden III). Header comment updated. `Mesen.opt` key count unchanged at 2. Total opt key count across all 8 cores unchanged at 27 (Genesis -1, PCSX +1, net 0); total cfg key count unchanged at 49. **README updates:** §1 Supported Cores table Beetle PCE Fast row Notes — appended `pce_fast_cdspeed = "4"` clause with v3.1 marker, Ys IV / Dracula X desync caveat, per-game revert guidance. §1 Supported Cores table Genesis Plus GX row — `.opt Keys` column `7` → `6`; Notes rewritten to flag `audio_filter` low-pass → off + inert lowpass_range key removal with v3.1 marker. §1 Supported Cores table Mesen row Notes — appended `mesen_reduce_dmc_popping` enabled → disabled clause with v3.1 marker, DPCM-heavy pop caveat, per-game restore guidance. §1 Supported Cores table mGBA row Notes — replaced `interframe_blending = "mix_smart"` preservation clause with `mgba_interframe_blending` disabled clause + `mgba_audio_low_pass_filter` disabled clause, both with v3.1 markers; flicker caveat moved to v3.1 clause. §1 Supported Cores table PCSX-ReARMed row — `.opt Keys` column `5` → `6`; Notes appended `pcsx_rearmed_frameskip_type` disabled → auto_threshold + new `pcsx_rearmed_frameskip_threshold = "33"` key with v3.1 marker, Spyro/THPS/Tekken benefit + frame-drop tradeoff explanation. §5 Shaders section — rewritten to reflect companion cfg state: gate on (`video_shader_enable = "true"`), no global preset set, users assign per-core via Save Core Preset; crt-easymode documented as recommended starting point rather than auto-loaded default. All other 11 `.cfg`/`.opt` file contents unchanged from v3.0. Version badge 3.0 → 3.1. Companion `retroarch-appletv4k` bumped to v3.1 (3 `retroarch.cfg` value changes: `fastforward_ratio` 4.0 → 0.0, `audio_resampler_quality` 2 → 1, `savestate_thumbnail_enable` true → false; global key count unchanged at 65; landing paragraph + §7 video/latency/additional tables + §7 save-state callout + §8 Shaders intro + §11 checklist updated to match).

2026-04-19  Ryan Musante

- v3.0: MAJOR version-sync release. Establishes lockstep versioning across companion repositories: both `retroarch-configs` and `retroarch-appletv4k` now share a single MAJOR.MINOR tag and will bump together on every future release regardless of which side contains the real file changes. Pre-sync state: `retroarch-configs` at v1.57, `retroarch-appletv4k` at v2.95 (the two repos had evolved independently since inception with asymmetric version numbering). No `.cfg`/`.opt` changes, no per-core key drift — all 16 file contents byte-identical to the v1.57 shipping state. **README updates:** §11 Versioning — policy text revised to document the lockstep convention explicitly, matching companion `retroarch-appletv4k` §13 byte-for-byte. Prior text: "This repository uses `vMAJOR.MINOR` (no patch component). `MAJOR` increments on incompatible structural changes (filesystem layout, breaking config schema, removed features)." New text prepends a lockstep-scope sentence and expands the MAJOR clause to include cross-repo version-sync events: "This repository uses `vMAJOR.MINOR` (no patch component) in lockstep with its companion repository — both `retroarch-appletv4k` and `retroarch-configs` share a single MAJOR.MINOR tag that increments together on every release, regardless of which side contains the real file changes. `MAJOR` increments on incompatible structural changes (filesystem layout, breaking config schema, removed features) or cross-repo version-sync events (e.g. v3.0 re-aligned the two repos after they had evolved independently at v2.95 / v1.57)." MINOR clause, CHANGELOG GNU-style-date convention, and 5-entry trim policy unchanged. Total cfg key count across all 8 cores unchanged at 49; total opt key count unchanged at 27. Version badge 1.57 → 3.0. Companion `retroarch-appletv4k` bumped to v3.0 (matching sync; §13 Versioning text revised identically; `retroarch.cfg` byte-identical to v2.95 shipping state).

2026-04-19  Ryan Musante

- v1.57: 2 `.cfg` files modified, 1 `.opt` file modified. Removes Mupen64Plus-Next per-core stutter-mitigation tax introduced in v1.56; applies two max-performance tunes to Beetle PCE Fast. **Removes (Mupen64Plus-Next.cfg, -2 keys):** `audio_latency = "64"` (Mupen now inherits global baseline; no per-core audio buffer padding). `video_max_swapchain_images = "3"` (Mupen now inherits global double-buffer `2`; no per-core output-latency penalty). Mupen64Plus-Next.cfg key count 7 → 5. Header comment reverted to "Tier 2 N64 frontend pins: video_threaded off, video_frame_delay_auto off, rewind off, run_ahead off" (drops the audio_latency + swapchain mention). Rationale: the v1.56 stutter mitigation penalized Mupen with additional audio latency and output latency — costs that only pay off if CPU underruns are actually occurring. The v1.56 `"all threads"` angrylion-multithread pin (retained) already extracts maximum parallelism from the A15. If underruns prove unavoidable in specific heavy ROMs, mitigation belongs in per-game Save Game Options, not in a per-core blanket pin. **Modifies (Beetle PCE Fast.opt, 1 value):** `pce_fast_cdimagecache = "disabled"` → `"enabled"` (loads the full CD image into RAM at content launch; eliminates disc seek latency during gameplay; startup load is slightly longer but gameplay is fully precached; aligns with PCSX-ReARMed `pcsx_rearmed_cd_readahead = "333000"` full-precache architecture; 64 GB ATV4K has ample cache for typical PCE CD image sizes 50–700 MB). Header comment updated. Opt key count `Beetle PCE Fast.opt` unchanged at 3. **Modifies (Beetle PCE Fast.cfg, 1 value):** `run_ahead_frames = "1"` → `"2"` (brings PCE Fast to parity with the 6 other Tier 1 cores which all run 2 frames). The prior 1-frame cap was justified in-repo as "CDROM seek determinism," but this was over-conservative — libretro/beetle-pce-fast-libretro issue #127 confirms 2-frame run-ahead works correctly on PCE CD games *as long as Run Ahead Secondary Instance is off*, and this repo has pinned `run_ahead_secondary_instance = "false"` on Beetle PCE Fast since v1.00. The v1.57 cdimagecache enable further eliminates disc-seek nondeterminism as a theoretical concern. Header comment updated. Cfg key count `Beetle PCE Fast.cfg` unchanged at 6. Total cfg key count across all 8 cores 51 → 49 (Mupen -2); total opt key count unchanged at 27. **README updates:** §1 Supported Cores Beetle PCE Fast row Notes — "Integer overscale 256×240 (+ 512-wide hi-res) @ 4K; Run Ahead 1 frame (CDROM seek determinism)" → "Integer overscale 256×240 (+ 512-wide hi-res) @ 4K; Run Ahead 2 frames (single instance; CD games verified safe per libretro/beetle-pce-fast-libretro#127); `pce_fast_cdimagecache = \"enabled\"` (full CD image precache to RAM; eliminates disc seek latency during gameplay)". §1 Supported Cores Mupen64Plus-Next row — `.cfg Keys` column `7` → `5`; Notes: dropped the per-core stutter-mitigation pin clause; added trailing note that Mupen inherits global low-latency posture — no per-core stutter-mitigation tax. §4 Frontend Override Keys table `audio_latency` row — Values Used reduced to `48` (PCSX-ReARMed pin only). §4 Frontend Override Keys — `video_max_swapchain_images` row removed entirely (no per-core pins remain; all cores inherit global double-buffer `2`). All other 13 file contents unchanged from v1.56. Version badge 1.56 → 1.57. Companion `retroarch-appletv4k` bumped to v2.95 (SYNC — no `retroarch.cfg` changes, README badge only; CHANGELOG entry describes the companion v1.57 changes for cross-reference; global key count unchanged at 65).

