2026-04-19  Ryan Musante

- v1.52: SYNC — no `.cfg` / `.opt` changes, no README changes. Version tag aligns the companion release channel with `retroarch-appletv4k` v2.90, which modifies `savestate_max_keep` from `"5"` to `"10"` in the global `retroarch.cfg` (doubles the auto-indexed state slot ring). Per-core frontend overrides do not touch `savestate_max_keep` — the new cap is inherited from global across all 8 cores. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.47 block removed). Version badge 1.51 → 1.52. Companion `retroarch-appletv4k` bumped to v2.90 (`savestate_max_keep` 5 → 10; global key count unchanged at 75).

2026-04-19  Ryan Musante

- v1.51: SYNC — no `.cfg` / `.opt` changes, no README changes. Version tag aligns the companion release channel with `retroarch-appletv4k` v2.89, which adds two `savestate_auto_*` pins (`savestate_auto_save = "true"`, `savestate_auto_load = "false"`) to the global `retroarch.cfg`. Per-core frontend overrides do not touch `savestate_auto_*` keys — the new behavior is inherited from global across all 8 cores. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.46 block removed). Version badge 1.50 → 1.51. Companion `retroarch-appletv4k` bumped to v2.89 (`savestate_auto_save` on, `savestate_auto_load` off; global key count 73 → 75).

2026-04-19  Ryan Musante

- v1.50: Documentation sync — no `.cfg` / `.opt` changes. §5 Shaders paragraph updated to reflect companion `retroarch-appletv4k` v2.88 shader swap: global `video_shader` path `../shaders/shaders_slang/crt/crt-aperture.slangp` → `../shaders/shaders_slang/crt/crt-easymode.slangp`; "as of companion v2.84" → "as of companion v2.88"; rationale added (integer-scale safe, no shader geometry, cleaner 4K phosphor mask); two references to "the global `crt-aperture` preset" in the same paragraph updated to `crt-easymode`. `handheld/lcd-grid-v2.slangp` guidance for mGBA retained unchanged. All 16 `.cfg` / `.opt` file contents, key counts (49 cfg + 28 opt), and key values unchanged from v1.49. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.45 block removed). Version badge 1.49 → 1.50. Companion `retroarch-appletv4k` bumped to v2.88 (global shader swap crt-aperture → crt-easymode; 3 drift-guard pins added: `video_vsync`, `sort_savefiles_enable`, `sort_savestates_enable`; README intro/§3/§4 directory phrasing trim; §7 VSync + Sort Save Files/States rows added; §8 shader documentation rewritten; §11 checklist updated; global key count 70 → 73).

2026-04-19  Ryan Musante

- v1.49: Documentation corrections — no key or value changes to `.cfg` / `.opt` files. (1) §4 Frontend Override Keys table `video_scale_integer` row — Purpose column reworded to clarify that global `retroarch.cfg` already sets `true`, so the per-core pin on Tier 1 + PCSX-ReARMed mirrors global as a drift-guard (not the enable gate); Mupen64Plus-Next has no per-core pin and inherits global `true` without a per-core overscale mode. Prior wording ("enable flag … without this bool, integer scaling is OFF") was technically true at the upstream-default layer but misleading given global already sets `true`. (2) README intro paragraph — "`video_scale_integer = "true"` enable flag" phrasing relaxed to "mirroring the global pin (drift-guard, not enable gate — global `retroarch.cfg` already sets `true`)" to match §4 rework. (3) v1.46 CHANGELOG companion key-count reference corrected from "global key count 70 → 71" to "65 → 66" (propagated error from trimmed v2.82 CHANGELOG entry that miscounted `retroarch.cfg` keys as 70 when actual was 65; companion bump delta +1 for `video_shader` is unchanged). (4) v1.47 CHANGELOG companion key-count reference corrected from "global key count 71 → 75" to "66 → 70" (same propagation; companion bump net +4 is unchanged). (5) Per-file descriptive header comment prepended to all 16 `.cfg` / `.opt` files (single `#` line, each under 120 chars) summarizing tier + system + key behaviors, e.g. `Beetle PCE Fast.cfg` → "Tier 1 PCE/TG-16 frontend overrides: integer overscale 256x240 @ 4K, run-ahead 1 frame (CDROM determinism)"; `FinalBurn Neo.opt` → "FBN core options placeholder: dipswitch and cheat settings maintained per-game only". Headers are parser-inert (RetroArch config parser treats `#`-prefixed lines as comments); key counts (49 cfg + 28 opt) and all key values unchanged from v1.48. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.44 block removed). Version badge 1.48 → 1.49. Companion `retroarch-appletv4k` bumped to v2.87 (documentation corrections: `retroarch.cfg` key-count arithmetic corrected through v2.84/v2.85/v2.86 entries; §7 Integer Scale row clarified as orthogonal to per-core `video_scale_integer_scaling` mode selector).

2026-04-18  Ryan Musante

- v1.48: Documentation corrections — no `.cfg` / `.opt` changes. (1) §1 Supported Cores Mupen64Plus-Next row — A15 core topology corrected from "2 P-cores + 4 E-cores" to "binned A15 with 2 P-cores + 3 E-cores, 1 E-core disabled" (Apple TV 4K 3rd Gen ships a binned A15 per Apple/EveryMac/Wikipedia/MacRumors; the full 2P+4E A15 is in iPhone 13/14, not ATV4K 3rd Gen; at core default "all threads" this yields 5 workers, not 6). (2) §11 Versioning — added explicit statement of the CHANGELOG 5-MINOR-version trim policy that prior entries had invoked but not documented. (3) v1.47 CHANGELOG `mupen64plus-angrylion-multithread` rationale — same A15 topology correction ("6 workers" → "5 workers"; "2P+4E" → "binned A15 2P+3E"); conclusion (`"2"` multithread pin) is unchanged because the P-core count is unchanged. (4) v1.46 CHANGELOG — fixed "6 cores" counting error for `crt-easymode.slangp` strip (actually 5: Beetle, FBN, Genesis, Mesen, Snes9x); restructured shader-strip enumeration to separate the 3 shader families (crt-easymode on 5 cores, zfast_crt on 2 Tier 2 cores, lcd-grid-v2 on mGBA = 8 cores total). (5) §1 Supported Cores Notes column — compressed all 8 rows to remove redundant boilerplate already canonical in intro and §4: dropped per-row literal `(`video_scale_integer = "true"`)` quotes (documented in §4), dropped per-row `run_ahead_secondary_instance = "false"` "(static-build inert pin)" boilerplate (documented in intro "`run_ahead_secondary_instance` pinned across all 8 cores" and §4 row), and tightened verbose phrasing ("Integer scaling enabled with overscale mode for X at 4K" → "Integer overscale X @ 4K"). Per-core specifics (frame counts, CDROM determinism, YM2612 choice, interframe_blending, issue refs, Angrylion multithread rationale, psxclock, cd_readahead) all retained. All per-core `.cfg` / `.opt` file contents, key counts (49 cfg + 28 opt), and key values unchanged from v1.47. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.43 block removed). Version badge 1.47 → 1.48. Companion `retroarch-appletv4k` bumped to v2.86.

