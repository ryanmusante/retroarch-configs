2026-04-17  Ryan Musante

- v1.46: Shader architecture reversal — remove `video_shader` line from all 8 per-core `.cfg` files; shaders now come from the global `video_shader = "../shaders/shaders_slang/crt/crt-aperture.slangp"` set in companion `retroarch-appletv4k` v2.84. Stripped paths: `crt/crt-easymode.slangp` (6 cores: Beetle, FBN, Genesis, Mesen, Snes9x — also the Tier 2 `zfast_crt.slangp` assignments on Mupen and PCSX), and `handheld/lcd-grid-v2.slangp` (mGBA). Per-core `.cfg` key counts all decrement by 1: Beetle 5→4, FBN 6→5, Genesis 5→4, Mesen 5→4, mGBA 5→4, Snes9x 5→4, Mupen 4→3, PCSX 5→4 (total 40→32 keys across 8 files). `README.md` intro paragraph: drop `video_shader` from the "explicit pins" list. §1 Supported Cores table: remove Shader column entirely; all 8 rows' `.cfg` Keys counts decremented; mGBA Notes extended with instruction to apply `handheld/lcd-grid-v2.slangp` via Save Core Preset for handheld-LCD aesthetic (reverses v1.41 mGBA-specific assignment). §4 Frontend Override Keys: remove `video_shader` row; add `video_shader` to the "intentionally not set per-core" list. §5 Shaders: rewritten to describe global default + optional per-core override via Save Core Preset UI (not `.cfg` entries). CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.41 block removed). Version badge 1.45 → 1.46. Companion `retroarch-appletv4k` bumped to v2.84 (adds global `video_shader = "../shaders/shaders_slang/crt/crt-aperture.slangp"`; global key count 70 → 71).

2026-04-17  Ryan Musante

- v1.45: Cleanup pass — remove dead sentinel `config/.gitkeep` (1 byte; redundant with 16 tracked `.cfg`/`.opt` files in same directory). `README.md` §2 File Structure: "The shipped ZIP contains 8 `.cfg`, 8 `.opt`, and `config/.gitkeep`." → "The shipped ZIP contains 8 `.cfg` and 8 `.opt` files, flat under `config/`." Shipped ZIP now contains exactly 16 files under `config/` (was 17). CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.39 + v1.40 blocks removed). Version badge 1.44 → 1.45. Companion `retroarch-appletv4k` bumped to v2.83 (SYNC + matching CHANGELOG trim).

2026-04-17  Ryan Musante

- v1.44: SYNC — no `.cfg` / `.opt` changes. README version badge bumped 1.43 → 1.44. Companion `retroarch-appletv4k` bumped to v2.82 (README↔`retroarch.cfg` documentation-sync pass: four mismatches fixed — §4 L182 literal-quote fix `savefile_directory` / `savestate_directory` from `"config/saves"` / `"config/states"` to actual cfg values `"../saves"` / `"../states"`; §7 L328 Cloud row `cloud_sync_driver` split from boolean sub-keys (empty string, not `"false"`); §1 L11 + §3 L125–126 File Browser / System-BIOS wording aligned to `../` relative-path scheme already used in §8 for `../shaders`; §7 Additional settings row added for `input_menu_toggle_gamepad_combo = "2"`). All 70 global `retroarch.cfg` keys now documented literally in companion README §7 tables or callouts. No per-core override changes.

2026-04-16  Ryan Musante

- v1.43: All 16 per-core `.cfg`/`.opt` files — strip all `#` comment and blank lines; total 112L → 65L. Keys, values, and key counts unchanged (54 keys across 16 files; per-core totals match v1.42 §1 table exactly: Beetle 5/3, FBN 6/0, Genesis 5/7, Mesen 5/2, mGBA 5/4, Snes9x 5/1, Mupen 4/8, PCSX 5/5). `README.md` §11 Versioning: CHANGELOG style description `kernel.org date<TAB>name` → `GNU ChangeLog style (YYYY-MM-DD<SP><SP>Author Name)` — actual file separator is double space. Version badge 1.42 → 1.43. Companion `retroarch-appletv4k` bumped to v2.81.

2026-04-15  Ryan Musante

- v1.42: Apple TV 4K optimization pass; 4 explicit `.opt` pins added for drift-guard consistency (all keys match prior upstream defaults — no behavior change). `config/Mupen64Plus-Next.opt` — add `mupen64plus-angrylion-multithread = "all threads"` (Angrylion uses all 6 A15 cores by default; explicit guards future thermal tuning). `config/PCSX-ReARMed.opt` — add `pcsx_rearmed_neon_enhancement_enable = "disabled"` (expensive upscale; no benefit on no-JIT A15; guards accidental flip). `config/mGBA.opt` — add `mgba_audio_low_pass_range = "60"` (pairs with `audio_low_pass_filter` already set). `config/Genesis Plus GX.opt` — add `genesis_plus_gx_render = "single field"` (lighter than double field on A15). `.opt` key counts: Genesis Plus GX 6 → 7, mGBA 3 → 4, Mupen64Plus-Next 7 → 8, PCSX-ReARMed 4 → 5.
- v1.42: `README.md` §1 Supported Cores table — `.opt` key counts updated for the 4 affected cores. Version badge 1.41 → 1.42. Companion `retroarch-appletv4k` bumped to v2.80 (SYNC bump; no `retroarch.cfg` changes).
