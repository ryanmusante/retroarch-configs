2026-04-19  Ryan Musante

- v1.49: Documentation corrections — no key or value changes to `.cfg` / `.opt` files. (1) §4 Frontend Override Keys table `video_scale_integer` row — Purpose column reworded to clarify that global `retroarch.cfg` already sets `true`, so the per-core pin on Tier 1 + PCSX-ReARMed mirrors global as a drift-guard (not the enable gate); Mupen64Plus-Next has no per-core pin and inherits global `true` without a per-core overscale mode. Prior wording ("enable flag … without this bool, integer scaling is OFF") was technically true at the upstream-default layer but misleading given global already sets `true`. (2) README intro paragraph — "`video_scale_integer = "true"` enable flag" phrasing relaxed to "mirroring the global pin (drift-guard, not enable gate — global `retroarch.cfg` already sets `true`)" to match §4 rework. (3) v1.46 CHANGELOG companion key-count reference corrected from "global key count 70 → 71" to "65 → 66" (propagated error from trimmed v2.82 CHANGELOG entry that miscounted `retroarch.cfg` keys as 70 when actual was 65; companion bump delta +1 for `video_shader` is unchanged). (4) v1.47 CHANGELOG companion key-count reference corrected from "global key count 71 → 75" to "66 → 70" (same propagation; companion bump net +4 is unchanged). (5) Per-file descriptive header comment prepended to all 16 `.cfg` / `.opt` files (single `#` line, each under 120 chars) summarizing tier + system + key behaviors, e.g. `Beetle PCE Fast.cfg` → "Tier 1 PCE/TG-16 frontend overrides: integer overscale 256x240 @ 4K, run-ahead 1 frame (CDROM determinism)"; `FinalBurn Neo.opt` → "FBN core options placeholder: dipswitch and cheat settings maintained per-game only". Headers are parser-inert (RetroArch config parser treats `#`-prefixed lines as comments); key counts (49 cfg + 28 opt) and all key values unchanged from v1.48. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.44 block removed). Version badge 1.48 → 1.49. Companion `retroarch-appletv4k` bumped to v2.87 (documentation corrections: `retroarch.cfg` key-count arithmetic corrected through v2.84/v2.85/v2.86 entries; §7 Integer Scale row clarified as orthogonal to per-core `video_scale_integer_scaling` mode selector).

2026-04-18  Ryan Musante

- v1.48: Documentation corrections — no `.cfg` / `.opt` changes. (1) §1 Supported Cores Mupen64Plus-Next row — A15 core topology corrected from "2 P-cores + 4 E-cores" to "binned A15 with 2 P-cores + 3 E-cores, 1 E-core disabled" (Apple TV 4K 3rd Gen ships a binned A15 per Apple/EveryMac/Wikipedia/MacRumors; the full 2P+4E A15 is in iPhone 13/14, not ATV4K 3rd Gen; at core default "all threads" this yields 5 workers, not 6). (2) §11 Versioning — added explicit statement of the CHANGELOG 5-MINOR-version trim policy that prior entries had invoked but not documented. (3) v1.47 CHANGELOG `mupen64plus-angrylion-multithread` rationale — same A15 topology correction ("6 workers" → "5 workers"; "2P+4E" → "binned A15 2P+3E"); conclusion (`"2"` multithread pin) is unchanged because the P-core count is unchanged. (4) v1.46 CHANGELOG — fixed "6 cores" counting error for `crt-easymode.slangp` strip (actually 5: Beetle, FBN, Genesis, Mesen, Snes9x); restructured shader-strip enumeration to separate the 3 shader families (crt-easymode on 5 cores, zfast_crt on 2 Tier 2 cores, lcd-grid-v2 on mGBA = 8 cores total). (5) §1 Supported Cores Notes column — compressed all 8 rows to remove redundant boilerplate already canonical in intro and §4: dropped per-row literal `(`video_scale_integer = "true"`)` quotes (documented in §4), dropped per-row `run_ahead_secondary_instance = "false"` "(static-build inert pin)" boilerplate (documented in intro "`run_ahead_secondary_instance` pinned across all 8 cores" and §4 row), and tightened verbose phrasing ("Integer scaling enabled with overscale mode for X at 4K" → "Integer overscale X @ 4K"). Per-core specifics (frame counts, CDROM determinism, YM2612 choice, interframe_blending, issue refs, Angrylion multithread rationale, psxclock, cd_readahead) all retained. All per-core `.cfg` / `.opt` file contents, key counts (49 cfg + 28 opt), and key values unchanged from v1.47. CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.43 block removed). Version badge 1.47 → 1.48. Companion `retroarch-appletv4k` bumped to v2.86.

2026-04-18  Ryan Musante

- v1.47: 11 corrections across 8 `.cfg` and 3 `.opt` files. **Adds:** `Mupen64Plus-Next.cfg` — `run_ahead_enabled = "false"` (no runtime HW-render guard in `runahead.c`/`runloop.c`, so per-core override is the only reliable suppression for GLideN64/angrylion HW-GL context state thrash on serialize/unserialize). 7 static cores (Beetle PCE Fast, Genesis Plus GX, Mesen, Mupen64Plus-Next, PCSX-ReARMed, Snes9x, mGBA) — `run_ahead_secondary_instance = "false"` (runtime default is `true` per [RA] `config.def.h:1473`; pin is inert on static tvOS builds per the `HAVE_DYNAMIC`/`HAVE_DYLIB` guard at `runloop.c:3126` but prevents behavior change on dynamic rebuilds). `FinalBurn Neo.cfg` — `run_ahead_secondary_instance = "true"` (two-instance mode required per core maintainer to avoid savestate audio buzz, [#16374](https://github.com/libretro/RetroArch/issues/16374)). 7 cores (Beetle PCE Fast, FinalBurn Neo, Genesis Plus GX, Mesen, PCSX-ReARMed, Snes9x, mGBA) — `video_scale_integer = "true"` (`SETTING_BOOL` default `false` is the enable flag — orthogonal to the existing `video_scale_integer_scaling = "1"` mode selector which is a `SETTING_UINT`. Without the bool flag, integer scaling is OFF regardless of mode value). `PCSX-ReARMed.cfg` — `rewind_enable = "false"` (defensive per-core guard; global default already `false` but pin prevents menu-toggle regression; PS1 rewind memory cost causes stalls on no-JIT A15). `PCSX-ReARMed.opt` — `pcsx_rearmed_cd_readahead = "333000"` (full-disk precache sentinel; eliminates CHD seek latency on flash storage; requires ~750 MB RAM headroom — A15 4 GB has budget). **Modifies:** `Mupen64Plus-Next.opt` — `mupen64plus-angrylion-multithread` value `"all threads"` → `"2"` (ATV4K 3rd Gen ships a binned A15 with 2 P-cores + 3 E-cores, 1 E-core disabled from the full 6-core A15; core default yields 5 workers that oversubscribe E-cores and harm frame pacing versus 2 P-core workers; `"2"` is a valid enum value). `mGBA.opt` — `mgba_interframe_blending` value `"mix"` → `"mix_smart"` (blends only flicker-effect frames, preserving motion clarity in non-flickering titles). **Removes:** `Mupen64Plus-Next.opt` — `mupen64plus-FrameDuping = "True"` stripped (FrameDuping requires `EnableFBEmulation=False` but core default is `EnableFBEmulation="True"` non-VC; with `EnableFBEmulation` at default `True`, `FrameDuping=True` is a no-op that can skew `video_frame_delay_auto` measurement windows). `Mupen64Plus-Next.opt` — `mupen64plus-ThreadedRenderer = "False"` stripped (matches unconditional core default at `libretro_core_options.h:187`; key was added v1.42 as a drift-guard pin, reclassified as redundant). `mGBA.opt` — `mgba_audio_low_pass_range = "60"` stripped (matches core default `"60"`; same drift-guard-pin reclassification). Per-core `.cfg` key counts: Beetle 4→6, FBN 5→7, Genesis 4→6, Mesen 4→6, Mupen 3→5, PCSX 4→7, Snes9x 4→6, mGBA 4→6 (total 32→49, +17). Per-core `.opt` key counts: Mupen 8→6, mGBA 4→3, PCSX 5→6 (total 30→28, −2). `README.md` intro: explicit-pins sentence expanded to enumerate new Mupen `run_ahead_enabled` pin, the 8-core `run_ahead_secondary_instance` split, and the `video_scale_integer` enable-flag / `video_scale_integer_scaling` mode-selector pairing. §1 Supported Cores table: all 8 core rows updated with new `.cfg` / `.opt` key counts and Notes columns extended with new pin descriptions. §4 Frontend Override Keys table: `run_ahead_enabled` Purpose column adds Mupen64Plus-Next; new `run_ahead_secondary_instance` row added; new `video_scale_integer` row added (separated from the existing `video_scale_integer_scaling` row which remains); `rewind_enable` Purpose column adds PCSX-ReARMed; "intentionally not set per-core" list: `run_ahead_secondary_instance` removed (now set on all 8 cores). CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.42 block removed). Version badge 1.46 → 1.47. Companion `retroarch-appletv4k` bumped to v2.85 (added `netplay_nat_traversal = "false"`, `audio_driver = "coreaudio"`, `input_overlay_enable = "false"`, `netplay_use_mitm_server = "false"`, `netplay_start_as_spectator = "false"`; removed `video_use_metal_arg_buffers`; global key count 66 → 70).

2026-04-17  Ryan Musante

- v1.46: Shader architecture reversal — remove `video_shader` line from all 8 per-core `.cfg` files; shaders now come from the global `video_shader = "../shaders/shaders_slang/crt/crt-aperture.slangp"` set in companion `retroarch-appletv4k` v2.84. Stripped paths across 8 cores: `crt/crt-easymode.slangp` from 5 cores (Beetle, FBN, Genesis, Mesen, Snes9x); `zfast_crt.slangp` from 2 Tier 2 cores (Mupen, PCSX); `handheld/lcd-grid-v2.slangp` from mGBA. Per-core `.cfg` key counts all decrement by 1: Beetle 5→4, FBN 6→5, Genesis 5→4, Mesen 5→4, mGBA 5→4, Snes9x 5→4, Mupen 4→3, PCSX 5→4 (total 40→32 keys across 8 files). `README.md` intro paragraph: drop `video_shader` from the "explicit pins" list. §1 Supported Cores table: remove Shader column entirely; all 8 rows' `.cfg` Keys counts decremented; mGBA Notes extended with instruction to apply `handheld/lcd-grid-v2.slangp` via Save Core Preset for handheld-LCD aesthetic (reverses v1.41 mGBA-specific assignment). §4 Frontend Override Keys: remove `video_shader` row; add `video_shader` to the "intentionally not set per-core" list. §5 Shaders: rewritten to describe global default + optional per-core override via Save Core Preset UI (not `.cfg` entries). CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.41 block removed). Version badge 1.45 → 1.46. Companion `retroarch-appletv4k` bumped to v2.84 (adds global `video_shader = "../shaders/shaders_slang/crt/crt-aperture.slangp"`; global key count 65 → 66).

2026-04-17  Ryan Musante

- v1.45: Cleanup pass — remove dead sentinel `config/.gitkeep` (1 byte; redundant with 16 tracked `.cfg`/`.opt` files in same directory). `README.md` §2 File Structure: "The shipped ZIP contains 8 `.cfg`, 8 `.opt`, and `config/.gitkeep`." → "The shipped ZIP contains 8 `.cfg` and 8 `.opt` files, flat under `config/`." Shipped ZIP now contains exactly 16 files under `config/` (was 17). CHANGELOG trimmed to last 5 minor versions per §11 policy (v1.39 + v1.40 blocks removed). Version badge 1.44 → 1.45. Companion `retroarch-appletv4k` bumped to v2.83 (SYNC + matching CHANGELOG trim).
