# Beetle PCE Fast.cfg v4.5 · Tier 1 PCE/TG-16 frontend overrides · integer overscale 256x240 @ 4K · run-ahead 2 frames (single instance — the second-instance mode hangs CD images, libretro/beetle-pce-fast-libretro#127 open) · 2 keys · paired with retroarch-appletv4k v4.5

# === Video (integer overscale) ===
video_scale_integer_scaling = "1"

# === Latency / Run Ahead ===
run_ahead_enabled = "true"
