# Beetle PCE Fast.cfg v3.25 · Tier 1 PCE/TG-16 frontend overrides · integer overscale 256x240 @ 4K · run-ahead 2 frames (single instance; CD-safe per libretro/beetle-pce-fast-libretro#127) · 2 keys · paired with retroarch-appletv4k v3.25

# === Video (integer overscale) ===
video_scale_integer_scaling = "1"

# === Latency / Run Ahead ===
run_ahead_enabled = "true"
