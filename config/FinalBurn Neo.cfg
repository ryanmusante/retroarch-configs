# FinalBurn Neo.cfg v3.24 · Tier 1 Neo Geo / Arcade (CPS1/2/3) frontend overrides · integer overscale 224p-304p @ 4K · run-ahead 2 + secondary instance · rewind pinned false (#16374 savestate audio buzz with run-ahead) · 4 keys · paired with retroarch-appletv4k v3.24

# === Video (integer overscale) ===
video_scale_integer_scaling = "1"

# === Latency / Run Ahead ===
run_ahead_enabled = "true"
run_ahead_secondary_instance = "true"

# === Saves / Savestates ===
rewind_enable = "false"
