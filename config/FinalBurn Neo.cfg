# FinalBurn Neo.cfg v4.1 · Tier 1 Neo Geo / Arcade (CPS1/2/3) overrides · integer overscale 224p-304p @ 4K · run-ahead 2 single-instance (per upstream FBN libretro README) · rewind pinned false (#16374) · 3 keys · paired with retroarch-appletv4k v4.1

# === Video (integer overscale) ===
video_scale_integer_scaling = "1"

# === Latency / Run Ahead ===
run_ahead_enabled = "true"

# === Saves / Savestates ===
rewind_enable = "false"
