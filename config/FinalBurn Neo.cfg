# FinalBurn Neo.cfg v5.2 · Tier 1 Neo Geo / Arcade (CPS1/2/3) frontend overrides · 3 keys · paired with retroarch-appletv4k v5.2

# === Video (integer overscale) ===
video_scale_integer_scaling = "1"

# === Latency / Run Ahead ===
run_ahead_enabled = "true"

# === Saves / Savestates ===
rewind_enable = "false"
