# Mupen64Plus-Next.cfg v5.2 · Tier 2 N64 frontend pins · 8 keys · paired with retroarch-appletv4k v5.2

# === Video (threading, frame delay) ===
video_threaded = "false"
video_frame_delay_auto = "false"

# === Audio ===
audio_sync = "true"
audio_latency = "64"

# === Latency / Run Ahead ===
run_ahead_enabled = "false"
run_ahead_secondary_instance = "false"

# === Saves / Savestates ===
autosave_interval = "0"
rewind_enable = "false"
