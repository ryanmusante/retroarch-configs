# Mupen64Plus-Next — Nintendo 64 · Tier 2 (Good)
# ~60-70% compatibility; complex titles may exhibit issues.
# video_frame_delay_auto=false below is the mitigation for libretro/RetroArch#14201
# (global is true). rewind is already disabled by the global rewind_enable=false,
# satisfying libretro/RetroArch#18300.
# Without JIT, run-ahead doubles cached_interpreter workload (preemptive frames also disabled below).
# Re-enable run_ahead_frames per-game for light titles (Mario 64, Kirby 64).

run_ahead_frames = "0"
preemptive_frames_enable = "false"
video_frame_delay_auto = "false"
video_threaded = "true"
# audio_latency = 64 ms: N64 cached interpreter has higher per-frame cost
# than PS1/DS interpreters; 48 ms causes underruns on heavy scenes.
audio_latency = "64"
# Lower resampler quality to reduce CPU load under cached interpreter.
audio_resampler_quality = "2"
# CRT shader: minimal GPU cost for interpreter-bound core.
video_shader = ":/shaders_slang/crt/zfast_crt.slangp"
