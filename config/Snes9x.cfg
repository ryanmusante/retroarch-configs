# Snes9x.cfg v3.25 · Tier 1 SNES frontend overrides · integer overscale 224p @ 4K · run-ahead 2 frames · 2 keys · paired with retroarch-appletv4k v3.25

# === Video (integer overscale) ===
video_scale_integer_scaling = "1"

# === Latency / Run Ahead ===
run_ahead_enabled = "true"
